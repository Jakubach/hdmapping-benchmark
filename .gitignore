# Input data (large files, download link in README)
data/

# Benchmark results (generated per-run)
outputs/

# Local-only files
monitor.sh
benchmark_description.*

# Cloned external repositories (instructions in README)
benchmarks/
livox_bag_aggregate/
mandeye_to_bag/
